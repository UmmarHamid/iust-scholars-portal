import Head from 'next/head';
import Logo from '@/components/Logo/Logo';
import { MdBuild, MdCall } from 'react-icons/md';
import { Button } from '@chakra-ui/button';
import { Stack } from '@chakra-ui/react';
import { styled } from 'styled-components';

const LoginButtonsWrapperStyled = styled.div`
  margin: auto;
  
`;

const MainStyled = styled.main`
display:flex;
justify-content:space-between;
align-items:center;
`

export const index = () => {
  return (
    <>
      <Head>
        <title>HomePage</title>
        <meta name='description' content='Generated by create next app' />
        <meta name='viewport' content='width=device-width, initial-scale=1' />
      </Head>
    <MainStyled>
      
        <Logo
          alt='IUST-logo'
          href='google.com'
          width={200}
          height={65}
          imgSrc='next.svg'
        />
        <LoginButtonsWrapperStyled>
          <Stack direction='row' spacing={4}>
            <Button leftIcon={<MdBuild />} colorScheme='blue' variant='outline'>
              Home
            </Button>
            <Button leftIcon={<MdCall />} colorScheme='pink' variant='solid'>
              SignUp
            </Button>
            <Button leftIcon={<MdCall />} colorScheme='green' variant='solid'>
              Login
            </Button>
          </Stack>
        </LoginButtonsWrapperStyled>
        </MainStyled>
    </>
  );
};
export default index;
